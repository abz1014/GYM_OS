# Vercel reads this at BUILD time, not runtime — changing it requires a redeploy, not a restart.
# Set it in the Vercel project's Environment Variables rather than committing a real value here.
VITE_API_BASE_URL=https://your-api.up.railway.app
